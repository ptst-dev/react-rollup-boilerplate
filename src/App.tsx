import React, { FC } from 'react';

import style from './App.module.scss';

export const App: FC = () => (
  <>
    <h1 className={style.header}>react-rollup-boilerplate</h1>
    <h2>It works!</h2>
  </>
);
